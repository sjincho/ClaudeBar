.build
.DS_Store
Package.resolved

# Tuist generated (regenerate with: tuist generate)
*.xcodeproj
*.xcworkspace
Derived/

# Xcode user data
xcuserdata/

# Build artifacts
build/
.serena/

# Personal Claude Code settings (per-project, never commit)
.claude/settings.local.json

# Local-only mise overrides (per-machine, e.g. code-signing identity)
mise.local.toml
.mise.local.toml
